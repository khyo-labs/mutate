{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @mutate/web build && cp -r apps/web/dist ./dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}